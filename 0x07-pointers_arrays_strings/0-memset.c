#include "main.h"

/**
 * _memset - Entry point
 * @s: pointer to put the constant
 * @b: constant byte
 * @n: bytes
 * Return: Always 0
 */

void simple_print_buffer(char *buffer, unsigned int size)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;

	return (s);
}
