#include <stdion>

/**
 * main - prints the lowercase alphabets in reverse
 * Return: Always 0
 */
int main(void)
{
	char le;

	for (le = 'z'; le >= 'a'; ch--)
		putchar(le);

	putchar('\n');

	return (0);
}
